def check():
    s = input()
    res = ""
    for i in range(len(s)):
        if s[i].isdigit():
            for _ in range(int(s[i])):
                res += s[i-1]
    print(res)
T=int(input())
for _ in range(T):
    check()
